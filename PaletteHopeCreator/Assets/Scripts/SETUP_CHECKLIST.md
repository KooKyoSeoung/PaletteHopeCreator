# 캐릭터 시스템 설정 체크리스트 ✅

게임이 제대로 작동하는지 확인하기 위한 체크리스트입니다.

## ✅ 필수 설정 항목

### 1. 데이터 준비
- [ ] `CharacterDatabase` ScriptableObject 생성
  - `Assets` 우클릭 → `Create` → `Game` → `Character Database`
  - 파일명: `CharacterDatabase` (원하는 이름으로 가능)
- [ ] 각 캐릭터의 `CharacterData` 생성
  - `Assets` 우클릭 → `Create` → `Game` → `Character Data`
  - 최소 1개 이상의 캐릭터 데이터 생성
  - 캐릭터 이름, 스프라이트, 능력치 설정
- [ ] `CharacterDatabase`에 모든 캐릭터 데이터 추가
  - `CharacterDatabase` 인스펙터에서 `All Characters` 리스트에 캐릭터들 추가

### 2. 시스템 GameObject 설정
- [ ] `CharacterInventory` GameObject 생성
  - 빈 GameObject 생성
  - `CharacterInventory` 컴포넌트 추가
  - `Character Database` 필드에 위에서 만든 데이터베이스 할당
- [ ] `GachaSystem` GameObject 생성
  - 빈 GameObject 생성
  - `GachaSystem` 컴포넌트 추가
  - `Character Database` 필드에 동일한 데이터베이스 할당
  - `Gacha Cost`: 가챠 1회 가격 설정 (기본 100)
  - 희귀도 확률 설정 (합계 100% 권장)

### 3. UI 설정
- [ ] 캐릭터 슬롯 프리팹 생성 (`CharacterSlotPrefab`)
  - Canvas에 UI 요소들 배치
  - `CharacterSlotUI` 스크립트 추가
  - 필요한 필드 할당 (Image, Text, Button 등)
  - 프리팹으로 저장
- [ ] `CharacterInventoryUI` 설정
  - Canvas에 `CharacterInventoryPanel` 생성
  - `CharacterInventoryUI` 컴포넌트 추가
  - 모든 필드 할당 (Panel, List Parent, Slot Prefab, Buttons 등)
- [ ] `GachaUI` 설정
  - Canvas에 `GachaPanel` 생성
  - `GachaUI` 컴포넌트 추가
  - 모든 필드 할당 (Panel, Buttons, Text 등)
- [ ] 결과 패널 설정
  - `GachaPanel` 안에 결과 표시용 UI 요소들 배치
  - `GachaUI`의 `Result Panel` 필드에 할당

### 4. GameManager 연결
- [ ] `GameManager`에 `openInventoryButton` 필드 설정
  - 메인 화면에 "캐릭터" 또는 "인벤토리" 버튼 생성
  - `GameManager`의 `Open Inventory Button` 필드에 할당

### 5. 테스트 도구 (선택사항)
- [ ] `TestHelper` 스크립트 추가
  - 빈 GameObject 생성
  - `TestHelper` 컴포넌트 추가
  - 게임 실행 중 **G 키** 누르면 가챠 포인트 1000개 지급
  - **Shift+R 키** 누르면 모든 데이터 리셋

## 🎮 테스트 방법

### 기본 테스트
1. 게임 실행
2. `G 키` 눌러서 가챠 포인트 지급 (또는 콘솔에서 `GachaSystem.Instance.GiveTestGachaPoints(1000)`)
3. 메인 화면에서 캐릭터 버튼 클릭
4. 캐릭터 인벤토리에서 보유 캐릭터 확인
5. 가챠 버튼 클릭
6. 가챠 실행하여 캐릭터 획득
7. 인벤토리에서 캐릭터 선택하여 플레이

### 기능 테스트
- [ ] 캐릭터 목록이 제대로 표시되는가?
- [ ] 캐릭터 선택 시 즉시 적용되는가?
- [ ] 가챠로 새 캐릭터를 얻을 수 있는가?
- [ ] 가챠 포인트가 차감되는가?
- [ ] 게임 재시작 시 보유 캐릭터가 유지되는가?
- [ ] 선택한 캐릭터가 저장되는가?

## 🐛 문제 해결

### 캐릭터가 표시되지 않는 경우
- `CharacterInventory`의 `Character Database` 필드가 할당되었는지 확인
- `CharacterDatabase`에 캐릭터 데이터가 추가되었는지 확인
- 콘솔에 에러 메시지 확인

### 가챠가 작동하지 않는 경우
- `GachaSystem`의 `Character Database` 필드가 할당되었는지 확인
- 가챠 포인트가 충분한지 확인 (G 키로 지급)
- 콘솔에 에러 메시지 확인

### UI가 표시되지 않는 경우
- Canvas가 씬에 있는지 확인
- UI Panel의 Active 상태 확인
- 모든 필드가 올바르게 할당되었는지 확인

### 캐릭터 선택이 적용되지 않는 경우
- `Player` 오브젝트에 `Player` 컴포넌트가 있는지 확인
- `CharacterInventory`가 `DontDestroyOnLoad`로 설정되어 있는지 확인
- 콘솔에 에러 메시지 확인

## 📝 추가 확인사항

- [ ] 모든 스크립트에 컴파일 에러가 없는가?
- [ ] 씬에 필요한 모든 GameObject가 있는가?
- [ ] PlayerPrefs가 정상 작동하는가? (게임 종료 후 재실행 시 데이터 유지)

## 🎯 완료!

모든 항목을 체크했다면 게임을 실행해서 테스트해보세요!

